import { T } from "../types/common";
import { Request, Response } from "express";
import { Email } from "../types/mailer";

const sendEmail: T = {};

sendEmail.signupMail = async(input: Email): Promise<void> => {
   
}